{
  "private": true,
  "name": "root",
  "scripts": {
    "test": "jest",
    "coveralls": "cat ./coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "coveralls": "^3.0.3",
    "jest": "^24.3.1",
    "lerna": "^3.13.1"
  },
  "dependencies": {
    "postcss": "^7.0.22",
    "postcss-scss": "^2.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
